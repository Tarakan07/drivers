const BASE_URL = "https://ergast.com/api";

const PREFIX = {
  f1: "/f1",
};
const SERVICES = {
  api: `/api`,
};

const Path = {
  drivers: "/drivers.json",
};

export { BASE_URL, PREFIX, SERVICES, Path };
